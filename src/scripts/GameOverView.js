/**
 * @description Create 'Game Over' view
 * @param {HTMLElement} context 
 * @param {number} questions 
 * @param {number} errors 
 * @param {[]} data 
 */
const GameOverView = (context, questions, errors, data) => {
    const element = document.createElement
}

export default GameOverView;